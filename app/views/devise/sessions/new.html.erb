<!-- ========== MAIN ========== -->
<main id="content" role="main" class="border-bottom">
  <!-- Login Form -->
  <div class="container space-top-2 space-top-md-4 space-bottom-2">
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "js-validate w-md-75 w-lg-50 mx-md-auto" }) do |f| %>
      <%= hidden_field_tag :job, params[:job] if params[:job] %>
      <%= hidden_field_tag :remember_me, true if devise_mapping.rememberable? %>
      
      <!-- Title -->
      <div class="mb-7">
        <h2 class="h3 font-weight-normal mb-0">Welcome <span class="font-weight-semi-bold text-success">back</span></h2>
        <p>Login to manage your account.</p>
      </div>
      <!-- End Title -->

      <!-- Form Group -->
      <div class="js-form-message form-group">
        <label class="form-label" for="signinSrEmail">Email address</label>
        <%= f.input :email,
                label: false,
                required: true,
                autofocus: true,
                placeholder: "Email",
                input_html: {
                  class: "form-control",
                  autocomplete: "email",
                  tabindex: 1,
                },
                data: {
                  rule_email: true,
                  msg: "Please enter a valid email address.",
                  errorClass: "u-has-error",
                  successCass: "u-has-success"
                } %>
      </div>
      <!-- End Form Group -->

      <!-- Form Group -->
      <div class="js-form-message form-group">
        <label class="form-label" for="signinSrPassword">
          <span class="d-flex justify-content-between align-items-center">
            Password
            <%= link_to "Forgot Password?", new_password_path(resource_name), class: "link-muted text-capitalize font-weight-normal", tabindex: 5 %>
          </span>
        </label>
        <%= f.input :password,
                label: false,
                required: true,
                input_html: {
                  class: "form-control",
                  autocomplete: "current-password",
                  tabindex: 2,
                },
                data: {
                  msg: "Your password is invalid. Please try again.",
                  errorClass: "u-has-error",
                  successCass: "u-has-success"
                } %>
      </div>
      <!-- End Form Group -->

      <!-- Button -->
      <button type="submit" class="btn btn-primary btn-block transition-3d-hover" tabindex="4">Log in</button>

      <div class="text-center mb-3 mt-3">
        <span class="u-divider u-divider--xs u-divider--text">OR</span>
      </div>

      <div class="d-flex">
        <%= link_to user_google_oauth2_omniauth_authorize_path, class: "btn btn-block btn-sm btn-soft-google transition-3d-hover ml-1 mt-0" do %>
          <span class="fab fa-google mr-1"></span>
          Google
        <% end %>
        <%= link_to user_github_omniauth_authorize_path, class: "btn btn-block btn-sm btn-soft-github transition-3d-hover ml-1 mt-0" do %>
          <span class="fab fa-github mr-1"></span>
          GitHub
        <% end %>
        <%= link_to user_linkedin_omniauth_authorize_path, class: "btn btn-block btn-sm btn-soft-linkedin transition-3d-hover ml-1 mt-0" do %>
          <span class="fab fa-linkedin mr-1"></span>
          LinkedIn
        <% end %>
      </div>
    <% end %>
  </div>
  <!-- End Login Form -->
</main>
<!-- ========== END MAIN ========== -->
